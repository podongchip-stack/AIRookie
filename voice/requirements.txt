# 요구 Python 버전: 3.11
# 직접 의존성 + 하위 의존성까지 전부 버전 고정되어 있어 재현 가능
#
# ⚠️ torch는 먼저 따로 설치한다. PyPI 기본 휠은 CPU 전용이라 GPU가 잡히지 않는다.
#   Windows + NVIDIA(RTX 50 시리즈 포함): pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
#   Mac: pip install torch==2.11.0   (MPS로 동작하나 ASR은 Mac에서 미검증)
# 그다음 pip install -r requirements.txt

# ===== 직접 의존성 (용도별) =====

# 모델 실행 — ASR(Qwen3-ASR)·구조화(HMM) 공통 (AI 처리)
torch==2.11.0
# Qwen3ASRForConditionalGeneration은 transformers 5.13 이상에만 있다
transformers==5.17.0
# ASR LoRA 어댑터 적용
peft==0.21.0

# 오디오 읽기 — wav는 soundfile, m4a 등은 PyAV(FFmpeg), 리샘플링은 scipy
soundfile==0.14.0
av==18.0.0
scipy==1.17.1
numpy==2.4.6

# 라이브 마이크 캡처
sounddevice==0.5.5

# 통화 요약 출력 JSON 스키마 검증 (hub·dashboard와의 계약 포맷 고정)
pydantic==2.13.4

# feature/hub로 통화 요약 JSON 전송·자가등록 (HTTP POST)
requests==2.34.2

# 웹 프레임워크: hub가 중계하는 통화 시작/종료 신호 수신 (app.py)
flask==3.1.3

# ===== 하위 의존성 (재현성을 위한 버전 고정, 위 패키지들이 자동으로 설치함) =====
accelerate==1.15.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
blinker==1.9.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
filelock==3.32.0
fsspec==2026.6.0
h11==0.16.0
hf-xet==1.5.2
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.32.0
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
networkx==3.6.1
packaging==26.0
psutil==7.2.2
pycparser==3.0
pydantic_core==2.46.4
Pygments==2.21.0
PyYAML==6.0.3
regex==2026.7.19
rich==15.0.0
safetensors==0.8.0
setuptools==78.1.0
shellingham==1.5.4
sympy==1.14.0
tokenizers==0.23.2
tqdm==4.70.0
typer==0.27.2
typing_extensions==4.16.0
typing-inspection==0.4.2
urllib3==2.7.0
Werkzeug==3.1.8

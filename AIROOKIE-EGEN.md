# AIRookie × E-Gen — 응급실 병상 정보 신뢰도 모델

> 작성: 2026-09-19, 갱신: 2026-09-23(재학습 실증 E12 · Relative Scale 성립 ·
> Disagreement 기각 · 부호 서술 교정 · ext0923 모델/프로토콜).
> GoldenLink(AIRookie) 관점에서 E-Gen 관련 내용만 발췌·정리한
> 자립 문서. **이 사본의 문중 상대 경로(REPORT.md, explore/ 등)는 모두
> C:\Dev\Modeling\Source-Action-Based Dynamic Reliability and Egress Estimation Model\ 기준.**
> 연구 전체 맥락은 `REPORT.md`, 실험 상세는 `explore/FINDINGS.md`
> §5-11 · §5-19 · §5-20 · §5-22 · §5-29 참조.
>
> **연동 상태: 2026-09-24 이 저장소에 서빙 연동 완료.** §5-1의 `pip install -e`
> 방식 대신 필요한 파일(serve.py·모델 JSON·리듬 테이블)을
> `info/Hospital_inform/info/reliability/`에 벤더링했다 — 사용법·재학습 교체
> 프로토콜은 그 폴더의 README.md 참고. hub 쪽 환산은 `hub/bed_reliability.py`.

## 1. 한 줄 요약

GoldenLink가 상수 **0.50**으로 두던 "E-Gen 정보 신뢰도"를, **병원별·시점별로
동적으로 계산하는 학습 모델**이 완성됐다 — "이 병원의 병상 숫자가 t분 뒤에도
유효할 확률"을 출력하며, 수집이 쌓일수록 재학습만으로 좋아진다.

## 2. 데이터 (이미 연동돼 있는 것)

- **원천**: AIRookie 수집기 스냅샷
  `C:\Dev\AIRookie\info\Hospital_inform\info\data\snapshots_nationwide\*.jsonl`
  (Windows 작업 스케줄러 `GoldenLink-EGen-Snapshot-Nationwide`, 20분 주기)
- 사용 필드: `ts`(관측시각) · `hpid`(병원) · `hvec`(응급실 가용 일반병상) ·
  `hvidate`(병원 신고시각 — ~~v1 피처 후보~~ **기각, §8 참조**) ·
  `hvs01`(기준 병상수 — **`Relative Scale` 피처 재료**)
- 학습에 쓴 축적: **16일** (2026-08-12~16 + 09-07~19), 전국 **443개 병원**,
  관측 45만 건 → claim-version **217,401개**
- 실측된 도메인 성질: 병상 값의 **수명 중위 20분**(= 폴링 간격 — 병상은 정말
  빨리 변한다), 관측의 48%에서 값이 바뀜

## 3. 라벨 설계 (egen-v0) — 등급을 정직하게

- claim = (병원, 가용병상수). 값이 바뀌는 순간마다 새 버전
- **무효화(EVENT)**: 다음 값 변화의 폭이 θ병상 이상 (θ ∈ {2, 3, 5}) —
  "5개라더니 곧 1개가 됨" = 이전 정보가 크게 어긋났음이 드러난 시점
- θ 미만의 잔변화 = 교체(검열), 관측 공백(>60분)마다 클레임 열 분리
- **중요 — 현재 라벨은 G1 등급**: 무효화 판정이 병원의 자기 신고 갱신에
  의존한다(독립 관측 아님). G2 승격 경로는 §7 참조

## 4. 모델과 성능

- 모델: XGBoost AFT (생존분석 — 검열 native 지원), 피처는 **내용을 안 보는
  행동 메타데이터**만: 병원의 평소 갱신 리듬, 버전 나이·간격, 변화폭, 시각/요일
- CPU 수 분 학습, 모델 파일 수 MB — GPU·클라우드 불필요

| θ(병상) | 본 모델 C-index | 갱신리듬 단독 | 문헌 최선 곡선 대비 IBS |
|---:|---:|---:|---|
| 2 | 0.691 | 0.646 | — |
| 3 | 0.764 | 0.701 | **2.1~2.4배 우위** |
| 5 | **0.829** | 0.740 | **1.8배 우위** (갱신빈도 휴리스틱 대비 34배) |

- 심각한 어긋남(θ↑)일수록 예측이 더 잘 됨 — 교통·기상·지도에서도 재현된
  이 연구의 교차 도메인 법칙
- 병원 리듬만으로도 순위는 어느 정도 나오지만(C 0.73~0.79), **캘리브레이트된
  확률**(예: "유효 확률 87%"가 실제로 87%)은 본 모델만 제공 — GoldenLink처럼
  확률을 소비하는 시스템에 결정적
- 타 도메인(버스) 모델의 무수정 이식은 안 됨(전이율 ≤33%) — **E-Gen 자체
  데이터 학습이 정답**임이 실측으로 확정된 상태. ~~원인은 version_no 부호가
  도메인 간 반대라는 것~~ → **2026-09-23 교정(FINDINGS §5-28)**: 부호는
  도메인 안에서도 인스턴스별로 갈리는 불안정한 지표라 원인 서술에서 내렸다
  (이식 불가라는 사실과 처방은 불변). 병상 도메인에선 **병원의 평소 리듬
  계열(갱신 간격 통계 묶음) 효과가 전 도메인 최강**(PDP·ALE 공히 +1.48 —
  단 리듬 피처들끼리 ρ=0.82로 얽혀 있어 개별 피처가 아닌 묶음으로 해석) —
  "이 병원답지 않은 움직임"이 핵심 신호라는 뜻

## 5. 산출물 (바로 쓸 수 있는 것)

`explore/data/` 안:

| 파일 | 내용 |
|---|---|
| `aft_egen_theta{2,3,5}.json` | 기보고(16일) 학습 모델 |
| `aft_egen_theta{2,3,5}_ext0923.json` | **최신 재학습 모델 (21일+, 2026-09-23)** — 서빙에는 이쪽 권장 |
| `egen_labels_v0.parquet` / `_ext0923.parquet` | 라벨·피처 데이터셋 (기보고 / 확장) |
| `egen_results_theta*.json` / `egen_baselines_theta*.json` | 성능·비교 수치 |

재현/재학습 — **기보고 산출물을 덮어쓰지 않는 프로토콜** (2026-09-23 확립):

```powershell
# 스냅샷 → 라벨 (--until = 파일 날짜 exclusive, 쓰는 중인 당일 제외)
python egen_pipeline.py --until <내일> --cutoff <경계일> --out egen_labels_v0_<tag>.parquet
# 재학습 (--tag가 모델·결과 파일에 접미사를 붙여 기존 파일 보호)
python egen_train.py --theta 3 --cutoff <경계일> --labels egen_labels_v0_<tag>.parquet --tag <tag>
```

월 1회 이 두 명령이면 갱신 완료. 재학습 우위는 실측 확인됨(§6 수칙 2).

### 5-1. 모델 출력 사용법 — **실물 함수가 있다** (2026-09-22)

개념 설명이 아니라 바로 호출할 수 있는 코드다. `infosurv` 패키지를 설치하면
된다(`pip install -e "<모델링 프로젝트 경로>"`):

```python
import xgboost as xgb
from infosurv import serve, fit

bst = xgb.Booster(); bst.load_model("data/aft_egen_theta3.json")
pred = fit.predict(bst, X)          # X = 병원별 행동 피처 (탄생 시점 관측 가능값만)

authority = serve.authority(pred, age=age_sec)             # 지금 믿어도 될 확률
ttl       = serve.ttl(pred, age=age_sec, threshold=0.8)    # 재확인까지 남은 초
r_arrive  = serve.at(pred, age=age_sec, horizon=eta_sec)   # 이송 도착 시점 유효 확률
```

| 값 | GoldenLink에서의 쓰임 |
|---|---|
| `authority` | **상수 0.50을 대체** — 병원별·시점별 동적 신뢰도 |
| `ttl` | 재확인·재조회 알림 시점 |
| `r_arrive` | **병원 추천 랭킹 가중치** — 지금이 아니라 *도착했을 때* 유효할 확률 |

유효 확인 이력이 있으면 `serve.authority(pred, age, confirmed_at=u)`로 조건부
갱신한다. 태생오류 분리를 쓰면 `pi=` 인자를 함께 넘긴다.

⚠ `ttl`·`at`의 시간 단위는 **학습에 쓴 단위와 같다**(E-Gen은 초).

## 6. 지금 해야 할 운영 수칙

1. **스냅샷 수집을 끊지 말 것** — 축적 = 성능 (그리고 §7의 NEDIS 소급 검증
   가능성을 예약하는 행위). 8월의 3주 공백 같은 단절이 가장 아프다
2. **주기적 재학습(월 1회) — 이제 직접 증거가 있다** (2026-09-23, E12):
   +27% 축적으로 재학습한 모델이 같은 test 창에서 동결 모델을 **세 시드
   전 조합에서** 이겼다(θ3 0.7355 vs 0.7246). 명령 두 줄, 수 분(§5 프로토콜).
   ⚠ 절대 C를 창 간 비교하지 말 것 — 주말 포함 창은 −0.02~0.03 낮게
   나온다(창 효과이지 성능 저하가 아님)

### 6-1. 운영 구성 — **전국 1개 모델이면 된다** (2026-09-22 실측, FINDINGS §5-20)

제품 설계에서 갈릴 수 있던 두 갈래가 실측으로 정리됐다.

| 물음 | 답 | 근거 |
|---|---|---|
| 권역별 모델이 필요한가 | **아니다. 전국 1개로 충분** | 16개 권역 중 13곳에서 전국 모델이 권역 native와 같거나 높음 (전이율 96~123%, **평균 104%**) |
| 신규 병원은 어떻게 하나 | **기존 모델을 그대로 적용** | 기존 355곳 학습 → 신규 88곳 zero-shot **C 0.6925 = 신규 native(0.6996)의 99%** |
| 신규 병원에 파인튜닝이 필요한가 | **불필요** | zero-shot(0.6925)이 이미 라벨 5,000개 파인튜닝(0.6915)과 동급 |

→ **운영 절차: 전국 1개 모델 + 월 1회 재학습. 신규 병원은 등록 즉시 적용하고
재학습 때 자연스럽게 포함시킨다.** 권역별 관리·cold-start 예외 처리·게이트
판정 같은 복잡한 장치가 **전부 불필요**하다.

> 참고: 라벨이 200개뿐인 상황에서는 파인튜닝이 의미 있다(scratch 0.6535 →
> 0.6913). 하지만 그럴 바엔 **기존 모델을 그대로 쓰는 게 더 낫다**(0.6925).

### 6-2. 단, 권역마다 예측 난이도가 다르다

권역별 native가 **C 0.6134 ~ 0.7295**로 갈린다(A26·A28이 쉽고 A29·A17·A16이
어렵다). 이것은 "모델을 나눠야 한다"가 아니라 **"같은 Authority 값이라도
권역에 따라 신뢰도가 다르다"**는 뜻이다. 신뢰도를 사용자에게 노출하거나
임계로 쓸 때 이 차이를 감안해야 한다. *(단 작은 권역의 낮은 값에는 표본
부족 효과가 섞여 있다 — FINDINGS §5-20 한계 5)*

## 7. 라벨 G2 승격 로드맵 — GoldenLink 폐루프

현재의 약점(G1: 병원 자기 신고 의존)을 없애는 경로:

```text
① E-Gen 병상 정보 + 본 모델 신뢰도 → ② GoldenLink 병원 추천 → ③ 실제 이송
        → ④ 도착 결과 기록 (수용/거부 + 시각)  ← G2 독립 관측 생성 지점
        → ⑤ 라벨로 환류, 재학습 → ① (쓸수록 정확해지는 플라이휠)
```

- 필요 구현은 경량: **도착 결과 입력 1회(수용/거부 버튼) + 운영 로그 테이블**
  — Part 2 스키마의 "운영 로그(요청·응답·도착결과)"에 자리 있음
- 부수 이점: 어떤 안내가 이송으로 이어졌는지 기록되므로 관측 편향 보정(IPCW,
  이미 구현된 도구)이 설계 단계에서 가능
- 외부 대안: NEDIS·소방청 구급활동일지(환자별 접수·전원·재이송)는 기관 신청·
  IRB·제공 시차 제약으로 **논문 단계의 사후 검증용** — 공개 재이송 통계는
  집계뿐이라 라벨 불가

## 8. v1 개선 후보 (우선순위순)

1. ~~**hvidate Refresh 피처**~~ → **기각 (2026-09-22, FINDINGS §5-19)**.
   신고 지연을 실측한 결과 **병원별 이질성은 크지만**(log 지연 분산의 25.1%,
   병원별 중위 4.1~34.3분) **무효화 예측력이 없었다** — 상대 지연 4분위의
   무효화율이 10.16 / 10.79 / 11.41 / 10.58%로 단조성이 없고, 절대 지연에서는
   방향이 오히려 반대다(`~10분` 10.85% vs `10~30분` 8.03%).
   원인: **`hvidate`는 병원 담당자의 신고가 아니라 시스템의 자동 갱신**이라
   각 병원이 시계처럼 규칙적이고(상대 지연 99분위 2.0배) "평소보다 늦는"
   사건이 없다. 재현: `explore/delay_probe.py --domain egen --until 2026-09-22`
1-1. **대체 후보 2종 → 실측 판정 완료 (2026-09-23, FINDINGS §5-29,
   재현: `explore/egen_feat_ext.py`)**:
   - ~~후보~~ → ✅ **`Relative Scale` 성립 — v1 채택 권장.**
     rel_scale(hvec/hvs01) + log_hvs01 추가 시 **θ2 +0.020 · θ3 +0.024
     (시드 3종 전승) · θ5 +0.054 (C 0.796→0.850)**. 이득이 θ와 함께
     커지는 것도 도메인 법칙과 정합. 음성 대조(열 셔플 → FULL 근방 회귀)
     통과. 갱신리듬 단독 대비 갭이 θ3 +0.060→+0.089로 벌어짐
   - ~~후보~~ → ❌ **`Disagreement History` 기각.** 중증수용 채널과의
     불일치 이력(XOR 확장률)은 전 θ에서 ±0.005 이내 — §5-16의 "피처로는
     유효할 것" 예상이 빗나갔다(정직 기록). 두 채널이 준독립인 것은
     맞지만 그 불일치가 병상 무효화를 예측하지는 않는다
2. ~~축적 연장 후 재학습~~ → **1차 완료 (2026-09-23, E12·FINDINGS §5-22)** —
   +27% 확장(21일+)에서 전 패턴 유지, 같은 창 비교 시 재학습이 동결 모델을
   시드 전 조합에서 이김(θ3 +0.011) — **월 1회 재학습 수칙의 직접 증거**.
   계속 연장은 유효
3. 폐루프 로그 설계 반영 (§7)
4. 지표 확장: hvec 외 중증수용(`getSrsillDissAceptncPosblInfoInqire`) 지표 추가

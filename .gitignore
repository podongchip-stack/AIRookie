# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
ehthumbs.db

# Editor
.vscode/
.idea/
*.swp

# Environment / secrets
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Python (hub 브랜치 언어로 확정됨)
__pycache__/
*.pyc
venv/
.venv/
*.egg-info/

# 이 저장소는 5개 브랜치가 하나의 작업 폴더를 공유하기 때문에, 다른 브랜치
# (dashboard 등) 작업 중 생긴 빌드 산출물이 hub 브랜치 체크아웃 상태에서도
# 보여서 실수로 커밋될 수 있다 (실제로 node_modules 전체가 커밋됐다가 GitHub
# 100MB 파일 크기 제한으로 push가 거부된 적 있음) — 방어적으로 추가해둔다.
node_modules/

# 테스트 매칭 실행 결과 (run_match.py가 생성, 재현 가능한 산출물)
data/test/output_*.json

# Misc
*.tmp
.cache/

# 추출 결과는 커밋하지 않는다.
#
# 실행할 때마다 새 파일이 쌓이는 폴더라 저장소에 두면 금세 지저분해진다.
# 더 중요한 이유는 내용이다 — 실제 병원 서류에서 뽑은 값(기관명, 당직 인력)이
# 들어가므로 공개 저장소에 올리면 안 된다.
#
# 재현: scripts/run_extract.py 를 같은 입력으로 다시 돌리면 된다.
*
!.gitignore

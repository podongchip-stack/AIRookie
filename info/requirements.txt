# 요구 Python 버전: 3.11 (예: conda create -n <가상환경명> python=3.11)
# 직접 의존성 + 하위 의존성까지 전부 버전 고정되어 있어 pip install -r requirements.txt 만으로 재현 가능

# ===== 직접 의존성 (용도별) =====

# 웹 프레임워크: 승인 액션 수신, 존 기반 병원 매칭 API 서빙 (규칙 기반)
flask==3.1.3

# 대시보드로의 실시간 바이탈/병원 매칭 결과 전송 (WebSocket)
flask-socketio==5.6.1
simple-websocket==1.1.0

# hv1(전문의 보유) / hvec(병상 현황) / hv2(중증질환별 수용 가능) 외부 API 호출 +
# feature/hub로 병원 정보 전송 (HTTP POST, send_to_hub.py)
requests==2.32.5

# 외부 API 키 등 환경 변수 관리 (Hospital_inform/.env: SUPABASE_URL, SUPABASE_KEY)
python-dotenv==1.2.1

# E-Gen 서비스키 승인 전까지 병원 정보 대체 DB (Hospital_inform/supabase/schema.sql,
# egen/client.py의 SupabaseEgenClient, send_to_hub.py에서 사용)
supabase==2.31.0

# Hospital_inform/info/schema.py, mapper.py의 HospitalInfo 검증
pydantic==2.13.4

# ===== 하위 의존성 (재현성을 위한 버전 고정, 위 패키지들이 자동으로 설치함) =====
anyio==4.14.2
bidict==0.23.1
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
deprecation==2.1.0
h2==4.4.1
h11==0.16.0
hpack==4.2.0
httpcore==1.0.9
httpx==0.28.1
hyperframe==6.1.0
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
multidict==6.7.1
packaging==26.0
postgrest==2.31.0
propcache==0.5.2
python-engineio==4.13.3
python-socketio==5.16.3
realtime==2.31.0
storage3==2.31.0
StrEnum==0.4.15
supabase-auth==2.31.0
supabase-functions==2.31.0
urllib3==2.7.0
websockets==15.0.1
Werkzeug==3.1.8
wsproto==1.3.2
yarl==1.24.5
